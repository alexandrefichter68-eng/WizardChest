diff --git a/node_modules/react-native-gesture-handler/lib/commonjs/web/tools/PointerEventManager.js b/node_modules/react-native-gesture-handler/lib/commonjs/web/tools/PointerEventManager.js
index 76eb808..88c91a1 100644
--- a/node_modules/react-native-gesture-handler/lib/commonjs/web/tools/PointerEventManager.js
+++ b/node_modules/react-native-gesture-handler/lib/commonjs/web/tools/PointerEventManager.js
@@ -36,7 +36,13 @@ class PointerEventManager extends _EventManager.default {
     const adaptedEvent = this.mapEvent(event, _interfaces.EventTypes.DOWN);
     const target = event.target;
     if (!POINTER_CAPTURE_EXCLUDE_LIST.has(target.tagName)) {
-      target.setPointerCapture(adaptedEvent.pointerId);
+      try {
+        target.setPointerCapture(adaptedEvent.pointerId);
+      } catch {
+        // Browsers can throw InvalidStateError here despite the spec saying this should fail
+        // silently (see the comment in pointerMoveCallback below) — safe to ignore, we retry on
+        // every subsequent pointermove until it succeeds.
+      }
     }
     this.markAsInBounds(adaptedEvent.pointerId);
     this.trackedPointers.add(adaptedEvent.pointerId);
@@ -86,7 +92,11 @@ class PointerEventManager extends _EventManager.default {
     // pointer moves until it succeeds.
     // God, I do love web development.
     if (!target?.hasPointerCapture(event.pointerId) && !POINTER_CAPTURE_EXCLUDE_LIST.has(target.tagName)) {
-      target.setPointerCapture(event.pointerId);
+      try {
+        target.setPointerCapture(event.pointerId);
+      } catch {
+        // See the try/catch in pointerDownCallback above.
+      }
     }
     const inBounds = (0, _utils.isPointerInBounds)(this.view, {
       x: adaptedEvent.x,
diff --git a/node_modules/react-native-gesture-handler/lib/module/web/tools/PointerEventManager.js b/node_modules/react-native-gesture-handler/lib/module/web/tools/PointerEventManager.js
index 6ce3b8f..9f7a9ae 100644
--- a/node_modules/react-native-gesture-handler/lib/module/web/tools/PointerEventManager.js
+++ b/node_modules/react-native-gesture-handler/lib/module/web/tools/PointerEventManager.js
@@ -31,7 +31,13 @@ export default class PointerEventManager extends EventManager {
     const adaptedEvent = this.mapEvent(event, EventTypes.DOWN);
     const target = event.target;
     if (!POINTER_CAPTURE_EXCLUDE_LIST.has(target.tagName)) {
-      target.setPointerCapture(adaptedEvent.pointerId);
+      try {
+        target.setPointerCapture(adaptedEvent.pointerId);
+      } catch {
+        // Browsers can throw InvalidStateError here despite the spec saying this should fail
+        // silently (see the comment in pointerMoveCallback below) — safe to ignore, we retry on
+        // every subsequent pointermove until it succeeds.
+      }
     }
     this.markAsInBounds(adaptedEvent.pointerId);
     this.trackedPointers.add(adaptedEvent.pointerId);
@@ -81,7 +87,11 @@ export default class PointerEventManager extends EventManager {
     // pointer moves until it succeeds.
     // God, I do love web development.
     if (!target?.hasPointerCapture(event.pointerId) && !POINTER_CAPTURE_EXCLUDE_LIST.has(target.tagName)) {
-      target.setPointerCapture(event.pointerId);
+      try {
+        target.setPointerCapture(event.pointerId);
+      } catch {
+        // See the try/catch in pointerDownCallback above.
+      }
     }
     const inBounds = isPointerInBounds(this.view, {
       x: adaptedEvent.x,
